local ds = (_PLATFORM == 'win') and '\\' or '/'

bootscript = _PATH .. 'scripts' .. ds .. 'system' .. ds .. 'boot.lua'
if _CONSOLE then
	-- index = 'index-cmd'
	index = 'index-exltest'
else
	index = 'index-test'
	silent = false
	if _PLATFORM == 'win' then
		windowsize={1280, 720}
	end
end
vsync = false
tpslimit = 0
